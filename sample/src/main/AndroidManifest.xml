<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Call State Permissions -->
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!-- Post Notification(FOR ANDROID API 33 or PLUS) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Screen Overlay -->
    <uses-permission android:name="android.permission.ACTION_MANAGE_OVERLAY_PERMISSION" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />

    <!-- Device Foreground Service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:name=".MyApplication"
        android:icon="@drawable/app_logo_"
        android:label="@string/app_name_tag"
        android:roundIcon="@drawable/app_logo_"
        android:supportsRtl="true"
        android:theme="@style/Base.Theme.Caller_ID"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <activity
            android:name=".ui.activity.SplashActivity"
            android:configChanges="uiMode"
            android:exported="true"
            android:label="@string/app_name"
            android:screenOrientation="nosensor"
            android:windowSoftInputMode="adjustNothing|adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.activity.MainActivity"
            android:configChanges="uiMode"
            android:exported="false"
            android:screenOrientation="nosensor"
            android:windowSoftInputMode="adjustNothing|adjustPan" />

        <activity
            android:name=".ui.activity.PermissionActivity"
            android:configChanges="uiMode"
            android:exported="false"
            android:screenOrientation="nosensor"
            android:windowSoftInputMode="adjustNothing|adjustPan" />

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713" />
    </application>

</manifest>